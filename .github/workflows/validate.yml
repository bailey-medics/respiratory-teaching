# Validate teaching content on pull request.
# Blocks merge if module metadata or MDX is invalid.
name: Validate

on:
  pull_request:

jobs:
  validate:
    uses: bailey-medics/teaching-tooling/.github/workflows/validate.yml@main
    with:
      org_id: respiratory
